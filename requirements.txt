# Core
Django>=5.1,<6
pillow==12.1.1
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
django-environ==0.13.0
django-cors-headers==4.7.0
PyJWT==2.11.0
sqlparse==0.5.3
asgiref==3.11.1

# Postgres driver (used when DATABASE_URL is set, e.g. on Render/Neon).
# Using psycopg (v3) instead of psycopg2-binary: psycopg2-binary has no
# prebuilt wheels yet for newer Python versions (e.g. 3.14), which makes
# pip fall back to building from source and fail with "pg_config not
# found". Django 4.2+ supports psycopg3 with the same postgresql ENGINE.
psycopg[binary]==3.2.10

# Production WSGI server (replaces manage.py runserver in prod)
gunicorn==23.0.0

# Serves static files (admin CSS, DRF browsable API) without needing nginx
whitenoise==6.11.0

# Очередь задач (вместо Celery + Redis)
django-q2==1.7.3

# Watchdog
watchdog==6.0.0

# HTTP
requests==2.32.3
